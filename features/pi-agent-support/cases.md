# Cases — Pi Agent support

Decisiones GATE 1: Pi packages como mecanismo (harness = Pi package instalable + `pi install npm:pi-subagents`); orquestador → `~/.pi/agent/AGENTS.md`; ZCode → notas + mejoras de bajo riesgo; instalador con menú + flags.

| ID | Caso | Fuente | Comportamiento esperado | Verificación | Tipo | Estado |
|---|---|---|---|---|---|---|
| C1 | Selector de target: sin flags muestra `(1) OpenCode (2) Pi Agent` y respeta la elección; EOF/no-TTY con flag no dado → error claro | pedido | Menú con `echo 2 \| ./install.sh` instala Pi; `echo` vacío falla con uso | unit (bash test, dirs temporales) | unit | ✅ |
| C2 | No-regresión OpenCode: `--target opencode` produce el mismo árbol/config que el install.sh actual | pedido ("tal cual") | Diff de árbol instalado + configs JSON idénticos (salvo lógica nueva aislada) | unit (bash test comparando con snapshot del comportamiento actual) | unit | ✅ |
| C3 | El repo es un Pi package válido: `package.json` declara `pi` manifest (prompts/skills/extensions) y `keywords: pi-package` | decisión 1 | Manifest parsea como JSON, rutas declaradas existen | unit (bash test: python json.load + chequeo de rutas) | unit | ✅ |
| C4 | `pi install <repo>` (local path) declara el package en el agent dir de Pi y Pi descubre sus recursos | decisión 1 | `~/.pi/agent/settings.json` contiene el package; `/feature` accesible como prompt; skills visibles | unit (bash test con PI_CODING_AGENT_DIR temporal + `pi install` real si hay `pi` CLI; si no, mock que valide el manifest) | unit | ✅ |
| C5 | Instalación Pi trae subagentes: corre `pi install npm:pi-subagents` (omite con aviso si no hay red/pi CLI, no aborta duro) | decisión 1 | Comando ejecutado; fallo → warning graceful | unit (bash test con mock de `pi`) | unit | ✅ |
| C6 | Orquestador inyectado en `~/.pi/agent/AGENTS.md` con merge no-destructivo y backup `.bak-droxon` | decisión 2 | AGENTS.md previo preservado (backup), bloque droxon presente una sola vez tras re-instalar | unit (bash test con agent dir temporal) | unit | ✅ |
| C7 | Prompt `/feature` adaptado a Pi en `pi/prompts/feature.md`: gates ⛔ y fases intactas; referencias OpenCode-only (fastloop_verify, task subagents, MCP yatt) sustituidas por equivalentes Pi (bash, npm:pi-subagents, pi-mcp-adapter) con nota de degradación | pedido + decisión 1 | El archivo existe, contiene los dos GATE, no referencia `fastloop_verify` como tool nativo | unit (bash test grep) | unit | ✅ |
| C8 | Extensión Pi `pi/extensions/droxon-harness.ts`: registra comando de verificación (equivalente mecánico del gate fastloop: corre el typecheck/build del proyecto y reporta) y pasa chequeo de sintaxis | pedido (hook portado) | Archivo parsea (bun/node --check equivalente) | unit (bash test: `bun build --no-bundle` o parse) | unit | ✅ |
| C9 | Deps en modo Pi: YATT (clon + bun install) y spec-kit igual que en OpenCode; `pi-mcp-adapter` sugerido para registrar YATT como MCP; fastloop y logo TUI omitidos con aviso explícito | pedido + checklist | Sin crash; avisos claros; YATT_HOME compartido | unit (bash test con DROXON_SKIP_DEPS=1 y sin skip) | unit | ✅ |
| C10 | Idempotencia en ambos targets: segunda corrida = mismo estado final, sin duplicados | checklist | Dos corridas, diff de árbol vacío | unit (bash test) | unit | ✅ |
| C11 | Flags no interactivos: `--target opencode|pi`, env `DROXON_TARGET`; flag inválido → uso y exit != 0 | decisión 4 | CI corre sin TTY | unit (bash test) | unit | ✅ |
| C12 | README actualizado: qué instala cada target, matriz de portabilidad, comandos pi equivalentes | checklist | README menciona Pi install path y limitaciones (fastloop/logo) | revisión directa | unit | ✅ |
| C13 | ZCode: `docs/zcode-notes.md` con hallazgos accionables citando secciones del repo ZCode + ≥1 mejora de bajo riesgo aplicada al harness (sin tocar el port Pi) | decisión 3 | Doc existe con citas verificables; mejora aplicada documentada | revisión del doc + diff acotado | unit | ✅ |
| C14 | Degradación graceful en Pi sin bun/npm/pi CLI: warnings, instalación de archivos del harness continúa | checklist | PATH sin bun → warnings, exit 0 con archivos instalados | unit (bash test con PATH recortado) | unit | ✅ |
